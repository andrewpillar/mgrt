name: Test

on:
  push:
    branches:
    - v4

  pull_request:
    branches:
    - v4

permissions:
  contents: read

jobs:

  unit:
    strategy:
      matrix:
        os:
        - ubuntu-latest
        - macos-latest
        - windows-latest

    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-go@v6
      with:
        go-version: '1.24'
    - run: go version

    - name: Run tests
      run: go test -v -coverprofile coverage ./...

